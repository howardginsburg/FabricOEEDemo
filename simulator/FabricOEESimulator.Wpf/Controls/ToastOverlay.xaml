<UserControl x:Class="FabricOEESimulator.Wpf.Controls.ToastOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ItemsControl ItemsSource="{Binding Toasts}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Margin="0,0,0,4" CornerRadius="4" Padding="10,8"
                        Background="#EE16213E" BorderThickness="3,0,0,0"
                        Cursor="Hand"
                        MouseLeftButtonDown="Toast_Click" Tag="{Binding}">
                    <Border.BorderBrush>
                        <SolidColorBrush x:Name="SevBrush" Color="#FFE74C3C"/>
                    </Border.BorderBrush>
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Severity}" Value="Maintenance">
                                    <Setter Property="BorderBrush" Value="#FF3498DB"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Severity}" Value="Info">
                                    <Setter Property="BorderBrush" Value="#FF2ECC71"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Text="{Binding Message}" Foreground="#FFE0E0E0" FontSize="11"
                                   TextWrapping="Wrap" MaxWidth="280"/>
                        <TextBlock Text="{Binding CreatedAt, StringFormat=HH:mm:ss}"
                                   Foreground="#FF8892A0" FontSize="9" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
